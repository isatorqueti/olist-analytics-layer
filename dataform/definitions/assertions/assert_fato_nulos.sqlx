config {
  type: "assertion",
  description: "Campos essenciais da fato_pedidos não podem ser nulos."
}

SELECT order_id
FROM ${ref("fato_pedidos")}
WHERE order_id    IS NULL
   OR customer_id IS NULL
   OR date_id     IS NULL